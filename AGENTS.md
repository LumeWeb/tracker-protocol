# AGENTS.md

## Module

`go.lumeweb.com/urma`

Go 1.26.

## Build & Test

```bash
go build ./...
go test -count=1 ./...
```

## Before Pushing

```bash
gofmt -l .
go vet ./...
go build ./...
go test -count=1 ./...
```

## Project Structure

```
├── claim.go          # UrmaClaim, SourceClaimID, MaxClaimScriptSize
├── codec.go          # EncodeClaim, DecodeClaim, ValidateClaimSize, GenerateSchema
├── envelope.go       # Envelope encoding/decoding, signing, verification
├── manifest.go       # ManifestPage (core paging type)
├── sia/              # Sia-specific package
│   ├── sia.go        # Manifest, ManifestBlobs, ManifestBlob, encode/decode helpers
│   ├── pagebuilder.go # PageBuilder, Plan, BuildChain, FitsClaim
│   └── schema.go     # JSON Schema generation
├── cmd/schemas/      # CLI: generates JSON Schema files
└── schemas/          # Generated schemas (gitignored)
```

## Coding Conventions

- **Core is agnostic.** Core types use `json.RawMessage` for location-specific data. No backend-specific types in core.
- **No redefined types.** Backend types come from their native packages directly (e.g. `go.sia.tech/indexd/slabs.SlabSlice`).
- **Child packages import parent, never reverse.**
- **No upload client.** Protocol defines data structures and helpers only.
- **No duplicated data.** Stream metadata lives only on page 0.
- **Location is a string type** (`type Location string`), not a numeric enum.
- Use `&Type{}` not `new(Type)`.
- No `_ = var` patterns.

## Generated Files

- `schemas/*.json` — generated by `go run ./cmd/schemas`. Gitignored. Do not edit manually.

## Dependencies

- `go.sia.tech/core` — Sia core types
- `go.sia.tech/indexd` — Slab/Sector types (`slabs.SlabSlice`, `slabs.PinnedSector`, `slabs.EncryptionKey`)
- `github.com/invopop/jsonschema` — JSON Schema reflection from Go structs
- `github.com/decred/dcrd/dcrec/secp256k1/v4` — ECDSA signing and verification

## Testing

- Framework: standard `testing` package
- Run: `go test -count=1 ./...`
- Tests live alongside source: `claim_test.go`, `envelope_test.go`, `manifest_test.go`, `sia/sia_test.go`, `sia/pagebuilder_test.go`, `sia/schema_test.go`
